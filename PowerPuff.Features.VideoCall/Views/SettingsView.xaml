<UserControl x:Class="PowerPuff.Features.VideoCall.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PowerPuff.Features.VideoCall.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{DynamicResource SettingContentStyle}">
        <DataGrid
            IsReadOnly="True"
            ItemsSource="{Binding Connections}"
            AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"
                                    Width="50"
                                    Binding="{Binding Id}"></DataGridTextColumn>
                <DataGridTextColumn Header="Name"
                                    Width="200"
                                    Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Skype"
                                    Width="*"
                                    Binding="{Binding Skype}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
